import React from "react"

const DesktopSelect = ({ optionGroups, valueGroups, onChange }) => {
  return (
    <div>
      {Object.keys(optionGroups).map((name) => (
        <></>
      ))}
    </div>
  )
}

export default DesktopSelect
