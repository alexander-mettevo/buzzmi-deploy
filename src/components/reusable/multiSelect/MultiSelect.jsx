import React from "react"

/**
 * Ну раз уж мы отталкиваемся от мобильного селекта то и сначала будем использовать его, а после добавлять уже по ходу дела и остальное
 */

const MultiSelect = ({ arrayData, setValues, values }) => {
  return (
    <div>
      <div className="d-none d-md-block"></div>
      <div className="d-md-none"></div>
    </div>
  )
}

export default MultiSelect
